[build]
dockerfilePath = "apps/indexer/Dockerfile"

[deploy]
startCommand = "ponder start --schema $RAILWAY_DEPLOYMENT_ID"
healthcheckPath = "/ready"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
